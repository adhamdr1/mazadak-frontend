import type { Auction } from '../types/auctions.types';

/**
 * Production Empty Mocks
 * All auction data is fetched live from NestJS GraphQL Backend & MongoDB
 */
export const MOCK_AUCTIONS: Auction[] = [];
